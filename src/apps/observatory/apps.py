from django.apps import AppConfig

class ObservatoryConfig(AppConfig):
    default_auto_field = 'django.db.models.BigAutoField'
    name = 'apps.observatory'
    verbose_name = 'Observatório de Direitos Humanos'
